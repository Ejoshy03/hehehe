# Romantic Tulip Login

Open `index.html` in any modern browser. No setup or backend is required.

The `images/` folder contains original SVG placeholder artwork, so the project runs fully offline. To use photographs instead, replace each SVG file with an image of the same name (or update the four image paths in `index.html`).
